**Exemplos de Código**

**C – Tipagem Estática e Fraca (`exemplo_c.c`)**

```c
#include <stdio.h>

int main() {
    int x = 5;
    float y = x + 2.5; // Aceita conversão implícita
    printf("Resultado: %f\n", y);
    return 0;
}
```
